{
  "name": "harness",
  "displayName": "NextEleven Harness — AI Coding Agent",
  "description": "NextEleven Harness — AI coding agent with Claude 4.x, GPT-5, Grok 4, and Qwen3 in VS Code",
  "version": "1.3.0",
  "license": "UNLICENSED",
  "publisher": "harness-ai",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": ["AI", "Chat", "Other"],
  "keywords": ["ai", "coding", "assistant", "claude", "gpt", "agent"],
  "icon": "media/icon.png",
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "harness.openChat",
        "title": "NextEleven Harness: Open Chat",
        "icon": "$(sparkle)"
      },
      {
        "command": "harness.inlineEdit",
        "title": "NextEleven Harness: Inline Edit (Cmd+I)",
        "icon": "$(edit)"
      },
      {
        "command": "harness.explainCode",
        "title": "NextEleven Harness: Explain Selected Code"
      },
      {
        "command": "harness.fixDiagnostics",
        "title": "NextEleven Harness: Fix Diagnostics"
      }
    ],
    "keybindings": [
      {
        "command": "harness.openChat",
        "key": "ctrl+shift+h",
        "mac": "cmd+shift+h"
      },
      {
        "command": "harness.inlineEdit",
        "key": "ctrl+i",
        "mac": "cmd+i",
        "when": "editorHasSelection"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "harness",
          "title": "NextEleven Harness",
          "icon": "$(sparkle)"
        }
      ]
    },
    "views": {
      "harness": [
        {
          "id": "harness.chatView",
          "name": "Chat",
          "type": "webview"
        }
      ]
    },
    "statusBar": [
      {
        "id": "harness.status",
        "name": "NextEleven Harness Status",
        "alignment": "right",
        "priority": 100
      }
    ],
    "configuration": {
      "title": "NextEleven Harness",
      "properties": {
        "harness.daemonSocket": {
          "type": "string",
          "default": "~/.harness/daemon.sock",
          "description": "Path to the NextEleven Harness daemon Unix socket."
        },
        "harness.model": {
          "type": "string",
          "default": "claude-sonnet-4-6",
          "description": "Default model to use."
        },
        "harness.autoConnect": {
          "type": "boolean",
          "default": true,
          "description": "Automatically connect to the NextEleven Harness daemon on startup."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.4.0",
    "@vscode/vsce": "^2.26.0"
  }
}
